services:
  fastapi-app:
    build: .
    container_name: fastapi-container-lite
    ports:
      - "3002:3002"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    command: uvicorn main:app --host 0.0.0.0 --port 3002 --reload
